# Consumet (only stream source). Use the ROOT of your Consumet API (e.g. https://consumet-xxx.vercel.app).
# In the Consumet project set TMDB_KEY (not TMDB_API_KEY). Providers: FlixHQ, HiMovies, Goku, SFlix, DramaCool.
# Watch is retried with server=vidcloud, upcloud when default returns no sources. (111movies is not in api.consumet.org routes.)
CONSUMET_API_BASE_URL=https://your-consumet.vercel.app

# TMDB API key (required for metadata and Consumet FlixHQ title fallback)
# Get one at https://www.themoviedb.org/settings/api
TMDB_API_KEY=your_tmdb_key_here

# Optional: port (default 3000) when running server.py locally
# PORT=3000
