# TMDB API key (required for metadata: browse, search, posters)
# Get one at https://www.themoviedb.org/settings/api
TMDB_API_KEY=your_tmdb_key_here

# Optional: port (default 3000) when running server.py locally
# PORT=3000
