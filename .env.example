# Consumet (primary stream source). Use the ROOT of your Consumet API (not /movies/flixhq).
# Example: https://consumet-livid-theta.vercel.app
# In the Consumet project on Vercel, add TMDB_API_KEY so meta/tmdb can look up by TMDB ID.
CONSUMET_API_BASE_URL=https://your-consumet.vercel.app

# TMDB API key (required for metadata and Consumet lookup)
# Get one at https://www.themoviedb.org/settings/api
TMDB_API_KEY=your_tmdb_key_here

# Optional: port (default 3000) when running server.py locally
# PORT=3000
